pipeline {
    agent any

    stages {
        stage('BE Unit Tests') {
            steps {
              dir('bugtracker-backend') {
                sh 'go test -v ./...'
              }
            }
        }
    }
}
